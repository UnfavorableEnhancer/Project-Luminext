class_name SkinEffectData

## Loads effects data from passed FileAccess **(must be used only by SkinData.load)**
func load(file : FileAccess) -> bool:
	return true

## Saves effects data to passed FileAccess **(must be used only by SkinData.load)**
func save(file : FileAccess) -> bool:
	return true
