class_name SkinSequenceData

## Loads sequence data from passed FileAccess **(must be used only by SkinData.load)**
func load(file : FileAccess) -> bool:
	return true

## Saves sequence data to passed FileAccess **(must be used only by SkinData.load)**
func save(file : FileAccess) -> bool:
	return true
