class_name SkinSFXData

## Loads sfx data from passed FileAccess **(must be used only by SkinData.load)**
func load(file : FileAccess) -> bool:
	return true

## Saves sfx data to passed FileAccess **(must be used only by SkinData.load)**
func save(file : FileAccess) -> bool:
	return true
